import { generateAndSendTicketPDF } from "./src/utils/generateAndSendTicketPDF.js";

// We won't fully execute it since we don't have mock data for Firebase, 
// but we can compile the file to catch any syntax errors.
console.log("generateAndSendTicketPDF module loaded successfully.");
